version: '3'

services:
  app:
    container_name: big-bolao-api
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/usr/app
    environment:
      - PORT=3333
    ports:
      - 3333:3333
